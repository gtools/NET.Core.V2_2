@model IEnumerable<NET.Core.V2_2.Models.SYS_Dept>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dept_Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dept_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dept_Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dept_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Dept_Code">Edit</a> @*|
                <a asp-action="Details" asp-route-id="@item.Dept_Code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Dept_Code">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
